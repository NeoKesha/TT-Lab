<UserControl x:Name="VectorEditorControl" x:Class="TT_Lab.Views.Composite.Vector2View"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TT_Lab.Controls"
             xmlns:viewModels="clr-namespace:TT_Lab.ViewModels.Composite"
             xmlns:cal="clr-namespace:Caliburn.Micro;assembly=Caliburn.Micro.Platform"
             d:DataContext="{Binding Source={d:DesignInstance viewModels:Vector2ViewModel}}"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <UniformGrid x:Name="FormationGrid" Columns="2">
        <local:LabeledTextBox Text="{Binding X}" VerticalAlignment="Center" TextBoxName="X" Margin="5,5,5,5" HorizontalAlignment="Stretch"/>
        <local:LabeledTextBox Text="{Binding Y}" VerticalAlignment="Center" TextBoxName="Y" Margin="5,5,5,5" HorizontalAlignment="Stretch"/>
    </UniformGrid>
</UserControl>
